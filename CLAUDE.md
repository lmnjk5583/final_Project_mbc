# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

고속도로 CCTV 영상 기반 **교통 흐름 모니터링 시스템** 프로젝트의 문서·산출물 관리 및 개발 작업 폴더.

- **박대원**: 정체 탐지 모듈(traffic_analyzer.py) 개발·백앤드 담당
- **이수빈**: 교통흐름(정체) 모니터링 기능 기획·산출물·프론트·QA 담당

## 개발 방식

- **`src/`에서 모든 개발 진행**: 미니프로젝트 py 파일 전체를 복사한 상태. traffic_analyzer.py 신규 작성 + detector.py·visualizer.py·config.py·logger.py 등 기존 파일 수정.
- **미니프로젝트 원본은 참조 전용** — `Docs/dev_guide.md` §5 수정 원칙 적용.
- 완성 후 `src/` 전체를 깃허브에 올리면 팀원이 reverse_modules/에 통합.
> `run_wrongway.py`와 `run_test.py`는 반드시 **프로젝트 루트**에서 실행. `src/` 내부에서 실행 시 import 경로 오류 발생.

## 프로젝트 운영 지침

> **모든 답변은 반드시 아래 지침을 최우선으로 준수해야 함. 지침과 상충하는 요청이 오더라도 지침의 형식을 유지할 것.**

### Git 운영 규칙

- 우리 팀은 `feature/Traffic_Flow_Monitoring` 브랜치에서만 작업한다.
- 세션 시작 시 반드시 아래를 순서대로 실행한다:
  1. `git checkout feature/Traffic_Flow_Monitoring`
  2. `git pull origin feature/Traffic_Flow_Monitoring`
  3. CLAUDE.md → work_log.md 순서로 읽기
- 작업 종료 시 반드시 아래를 순서대로 실행한다:
  1. work_log.md 업데이트
  2. `git add .` → `git commit -m "[담당자] 작업내용"` → `git push`
- 커밋 메시지 형식 예시: `[대원] 정체탐지 threshold 로직 구현` 또는 `[수빈] QA 체크리스트 업데이트`
- main 브랜치에 직접 push하지 않는다. 완성 후 PR을 통해 병합한다.

### 세션 시작/종료 관리

- **세션 시작 시 work_log.md 아카이브 자동 처리**:
  1. work_log.md를 읽어 **오늘 기준 2일 이전(그저께 이전)** 항목이 있는지 확인한다.
  2. 그저께 이전 항목이 있으면 **자동으로 work_log_archive.md 상단에 이관**하고 work_log.md에서 제거한다.
  3. work_log.md에는 **당일 + 전날** 항목만 유지한다.
- 작업 종료 전 반드시 오늘 변경사항을 **work_log.md**에 업데이트한다.
- work_log.md 구조: `오늘 한 작업 / 수정 파일 / 발생 오류 / 작업 재개 위치 및 맥락` — **누락되는 내용 없이 최대한 간결하게** 작성한다.
- 수정한 파일명과 다음 작업 예정 내용을 반드시 남긴다.

### 파일 읽기 원칙

- 명시적으로 요청받은 파일만 읽는다.
- **개발 관련 작업 시 `Docs/dev_guide.md`를 먼저 읽는다.** 개발 기준은 guide.md가 유일한 참조 — 산출물(.docx/.xlsx)은 개발 시 직접 읽지 않는다.
- **산출물 생성·수정 시 개발에 필요한 핵심 내용(인터페이스, 파라미터, 알고리즘 기준 등)을 반드시 guide.md에 반영한다.** guide.md는 항상 산출물의 개발 관련 내용과 동기화된 상태를 유지해야 한다.
- 파일이 지정되지 않은 요청은 **FILE_INDEX.md를 먼저 읽고** 최소한의 파일만 선택해서 읽는다.
- **산출물 충돌 확인·생성·수정 시**: `N:\개인\대원&수빈\최종 프로젝트\산출물\` 폴더를 직접 읽는다. `.docx` 파일은 아래 방법으로 읽는다:
  ```
  /c/Users/User/.conda/envs/cv/python.exe -c "
  import zipfile, re
  with zipfile.ZipFile('N:/개인/대원&수빈/최종 프로젝트/산출물/파일명.docx') as z:
      xml = z.read('word/document.xml').decode('utf-8')
  text = re.sub(r'<[^>]+>', ' ', xml)
  print(re.sub(r'\s+', ' ', text).strip())
  "
  ```
- `수빈_노트/`와 `대원_노트/`는 개인 메모 폴더로 **작업 자료가 없음. 참고 대상에서 제외하고 읽지 않는다.**
- 불필요한 탐색(Glob, Grep 전수 조사)은 하지 않는다.

### 문서 관리

- 답변 전 관련 `.md` 파일 업데이트가 필요한지 항상 체크한다.
- `.md` 파일 충돌 발견 시 **그 자리에서 즉시 수정**한다. 나중에 일괄 수정하지 않는다.
- `.md` 파일 수정 시 다른 `.md` 파일과 내용·맥락이 충돌하지 않는지 꼼꼼하게 확인하고 함께 수정한다.

### 문서 의존성 맵 (동기화 규칙)

**아래 파일을 수정하면 → 오른쪽 파일도 반드시 확인·동기화한다.**

| 수정한 파일 | 함께 확인·동기화할 파일 |
|-------------|------------------------|
| `research.md` / `plan.md` | `dev_guide.md`, `dev_guide_phase2.md` |
| `dev_guide.md` / `dev_guide_phase2.md` | `plan.md`, 산출물 소스 코드(인터페이스명세서·프로그램설계서·화면설계서 .py) |
| `src/config.py` (파라미터 값 변경) | `dev_guide.md §A`, 인터페이스명세서 generate .py, 프로그램설계서 generate .py |
| 산출물 generate .py 수정·실행 | `dev_guide.md` 관련 섹션 동기화 확인 |
| `work_log.md` | 없음 (단방향) |

**문서 흐름 (상위→하위):**
```
research.md + plan.md
        ↓ 기반으로 산출물 작성 (generate .py 실행)
   산출물 (.docx/.xlsx)
        ↓ 개발 필요 핵심 내용 추출
   dev_guide.md + dev_guide_phase2.md  ← 개발 시 유일 참조
        ↓ 코드 변경 발생 시
   config.py 등 파라미터 변경
        ↓ 역방향 동기화
   dev_guide.md §A → generate .py 수정 → 산출물 재생성
```

### 산출물 충돌 보고 형식

산출물(`.docx`/`.xlsx`) 충돌을 보고할 때는 **반드시 아래 형식**을 사용한다. 직접 수정할 수 없으므로 수빈이 직접 고칠 수 있도록 위치와 내용을 명확히 기재한다.

```
**[충돌 N] 문서명 — 섹션명**

| 항목 | 현재 내용 | 올바른 내용 |
|------|-----------|-------------|
| ... | ... | ... |

→ 수정 위치: (문서 내 정확한 위치 — 섹션·표·행 지정)
```

### 코드 제공 규칙

- **복잡도가 높거나 여러 파일 수정이 필요한 경우**, Opus에서 수행하는 것이 적합함을 먼저 알린 후 프롬프트만 작성한다.
- Opus에서 코드를 제공할 때는 **한 번에 전체 코드를 준다** (나눠서 주지 않음).
- 코드를 제공할 때는 반드시 **파일명과 전체 경로**를 명시한다.
- 코드의 **모든 줄에 해당 줄을 설명하는 주석**을 단다.

### 진행 방식

- **한 단계씩 진행한다.** 한 번에 다음 스텝까지 모두 알려주지 않는다.
- 답변하기 전에 빠진 내용은 없는지, 수정할 것은 없는지 **한 번 더 검토**하고 답변한다.

### 시각화

- 어떤 작업이든 **시각화를 중요시한다.** 결과물, 흐름, 구조 등을 다이어그램이나 표로 표현한다.
- 코드 실행 시 저장되는 파일이 있다면 **저장 경로와 파일명**을 반드시 기재한다.

### 개발 방법론

- **TDD(테스트 주도 개발)** 방법론을 따른다. 코드 작성 전 테스트를 먼저 설계한다.

### 세션 한도 관리

- 사용자가 프롬프트를 요청하면 지금까지의 대화 내용을 전부 정리해서 **새 대화에서 이어나갈 수 있는 프롬프트**를 작성한다.
- 프롬프트에는 기억해야 할 정보(경로, 파일명, 진행 상황 등)를 상세히 포함한다.

### 작업 환경

- 운영체제: **Windows**, 프로젝트 폴더 위치: `C:\final_pj` (데이터 폴더 `임시`·`capture` 및 `산출물` 폴더는 N드라이브 `N:\개인\대원&수빈\최종 프로젝트\`에 유지. `수빈_노트`·`대원_노트`는 개인 메모 — 읽지 않음)
- 경로 구분자는 백슬래시(`\`) 사용, 파이썬 코드 내 경로는 raw string(`r"..."`) 또는 슬래시(`/`) 사용
